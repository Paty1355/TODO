<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Todolist.MainPage">

    <StackLayout>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Entry x:Name="tresc" Placeholder="Podaj treść TODO"/>
            <Button Grid.Column="1" Text="Dodaj TODO" Clicked="Button_Clicked"/>
        </Grid>
        <BoxView Color="Crimson" HeightRequest="1"/>
        <ListView x:Name="lista" SeparatorColor="Crimson" HasUnevenRows="True">
        </ListView>
    </StackLayout>

</ContentPage>
